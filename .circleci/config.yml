version: 2.1
executors:
    docker-publisher:
      environment:
          IMAGE_NAME: radekfalhar/librarywebsite
      docker:
          - image: circleci/buildpack-deps:stretch
jobs:

# docker build inspired by https://circleci.com/blog/using-circleci-workflows-to-replicate-docker-hub-automated-builds/
  docker_build:
    executor: docker-publisher
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Build Docker image
          command: |
            docker build -t $IMAGE_NAME:latest -f LibraryWebsite/Dockerfile .
      - run:
          name: Archive Docker image
          command: docker save -o image.tar $IMAGE_NAME
      - persist_to_workspace:
          root: .
          paths:
            - ./image.tar

  test_dotnet:
    docker:
      - image: mcr.microsoft.com/dotnet/core/sdk:3.0.100-preview8-buster

    steps:
      - checkout
      - run:
          name: Run Tests
          command:  |
            dotnet --version
            dotnet test LibraryWebsite.Test -c Release --logger "trx"
      - run:
          name: Convert test results from trx to junit
          when: always
          command: |
              export PATH="$PATH:/root/.dotnet/tools"
              dotnet tool install -g trx2junit
              trx2junit **/TestResults/*.trx --output TestResults/dotnet
      - store_test_results:
          path: TestResults
      - store_artifacts:
          path: TestResults
          destination: TestResults

  test_angular:
    docker:
      - image: circleci/node:8-browsers
    steps:
      - checkout
      - restore_cache:
          key: LibraryWebsite-client-{{ .Branch }}-{{ checksum "LibraryWebsite/ClientApp/package-lock.json" }}
      - run: cd LibraryWebsite/ClientApp/ && npm install
      - save_cache:
          key: LibraryWebsite-client-{{ .Branch }}-{{ checksum "LibraryWebsite/ClientApp/package-lock.json" }}
          paths:
            - "LibraryWebsite/ClientApp/node_modules"
      - run: 
          command: cd LibraryWebsite/ClientApp/ && npm run test -- --no-watch --no-progress --browsers=ChromeHeadlessCI
          environment:
            JUNIT_REPORT_PATH: ../TestResults
            JUNIT_REPORT_NAME: test-results-client.xml
      - store_test_results:
          path: LibraryWebsite/ClientApp/TestResults
      - store_artifacts:
          path: LibraryWebsite/ClientApp/TestResults

  test_dotnet_database:
    machine: true
    steps:
        - checkout
        - run:
            name: Run database tests
            command: |
              docker-compose -f docker-compose.databasetest.yml up --build --abort-on-container-exit --exit-code-from database_test
        - store_artifacts:
            path: TestResults
            destination: TestResults
        - store_test_results:
            path: TestResults

  end_to_end_test:
    machine: true
    steps:
        - attach_workspace:
            at: /tmp/workspace
        - checkout
        # - setup_remote_docker
        - run:
            name: Load archived Docker image
            command: docker load -i /tmp/workspace/image.tar
        - run:
            name: Run integration tests
            command: |
              docker-compose -f docker-compose.endtoendtest.yml up --build --abort-on-container-exit --exit-code-from end_to_end_test
        - store_artifacts:
            path: TestResults
            destination: TestResults
        - store_test_results:
            path: TestResults

  docker_publish-latest:
    executor: docker-publisher
    steps:
      - attach_workspace:
          at: /tmp/workspace
      - setup_remote_docker
      - run:
          name: Load archived Docker image
          command: docker load -i /tmp/workspace/image.tar
      - run:
          name: Publish Docker Image to Docker Hub
          command: |
            echo "$DOCKERHUB_PASS" | docker login -u "$DOCKERHUB_USERNAME" --password-stdin
            docker push $IMAGE_NAME:latest

workflows:
  version: 2
  build_and_publish:
    jobs:
      - docker_build
      - test_dotnet:
          requires:
              - docker_build
      - test_angular:
          requires:
              - docker_build
      - test_dotnet_database:
          requires:
              - docker_build
      - end_to_end_test:
          requires:
            - docker_build
      - docker_publish-latest:
          requires:
            - test_dotnet
            - test_angular
            - test_dotnet_database
            - end_to_end_test
          context: "Docker hub credentials"
          filters:
            branches:
              only: master